# Phase 01 — shared behavior

**Purpose:** orchestrate pure cross-client rules, validation and shared models.

## Planned subphases

- One `TS-*` per independent rule, validator, mapper or state transition.
- Shared setup used by several scenarios belongs in one `EN-*`.

## Exit checkpoint

Each selected rule has focused unit evidence or a documented exception.
