# Phase 05 — Angular UI

**Purpose:** orchestrate Angular components/templates, user interactions,
accessibility and SSR-visible states.

## Planned subphases

- One `TS-*` per independently reviewable interaction or visible state.
- Separate browser-only behavior from SSR/hydration responsibilities.
- Missing Angular test harness is an `EN-*`, not an invalid Red.

## Exit checkpoint

In-scope Angular interactions, template accessibility and SSR/browser risks
have focused evidence.
