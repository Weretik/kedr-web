# Phase 03 — React Web UI

**Purpose:** orchestrate Admin React user interactions and visible states.

## Planned subphases

- One `TS-*` per independently reviewable user interaction or visible state.
- Use Vitest and React Testing Library only where the selected Nx project
  actually exposes that tooling.

## Exit checkpoint

In-scope web interactions and accessibility behavior have component evidence.
