# Phase 06 — navigation and platform

**Purpose:** orchestrate routes, deep links, permissions, storage and
browser/native adapters.

## Planned subphases

- Separate `TS-*` files for navigation, deep-link parsing, permission state,
  storage behavior and each platform adapter responsibility.
- Missing harness/tooling is an `EN-*`, not an invalid Red.

## Exit checkpoint

Each integration boundary and denied/error/fallback state in scope has evidence.
