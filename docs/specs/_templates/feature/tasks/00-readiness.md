# Phase 00 — readiness

**Purpose:** resolve scope, tooling and prerequisites before behavioral tasks.

## Planned subphases

- `EN-*` for a missing test harness, fixture boundary or shared prerequisite.
- `TS-*` for one specification/verification responsibility when needed.

## Exit checkpoint

Required scenarios, paths, targets and dependencies are concrete; unresolved
product decisions remain explicit blockers.
