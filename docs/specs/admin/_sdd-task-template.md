# SDD: <краткое название задачи>

> Скопируйте этот файл в `docs/specs/admin/<домен>/<название>.md`, замените плейсхолдеры и удалите неактуальные разделы. Для изменений визуальной системы также сверяйтесь с `docs/specs/admin/template-migration.md`.

## Цель

<Какую пользовательскую или бизнес-проблему решает задача и какой ожидается результат.>

## Границы

- **In scope:** <что необходимо реализовать.>
- **Out of scope:** <что намеренно не меняется.>
- **Затрагиваемые библиотеки:** `<apps или libs>`.
- **Зависимости:** <API-контракт, права доступа, дизайн-решение либо «нет».>

## Сценарии

1. **<Название сценария>.** Given <начальное состояние>, When <действие>, Then <результат>.
2. **<Пограничный сценарий>.** Given <начальное состояние>, When <действие>, Then <результат>.

## Контракты и данные

- **Источник данных:** <endpoint / RTK Query service / статическое состояние до появления API>.
- **Типы:** <модели, обязательные поля, nullable-значения>.
- **Состояния:** loading, empty, error и success — <как выглядят и откуда берутся>.
- **Авторизация:** <нужные permissions и поведение при `401`/`403`>.

## UI и маршрутизация

- **Route:** `<путь>` или «маршрут не меняется».
- **Компоненты:** <страницы, layout, существующие визуальные компоненты>.
- **Адаптивность:** <breakpoints и поведение на mobile>.
- **Доступность:** <семантика, доступные имена, focus, keyboard-сценарий>.
- **Тема:** <используемые MUI tokens; фирменные цвета Kedr не изменяются>.

## Технический план

1. <Минимальное изменение в data-access/model.>
2. <Изменение feature или layout.>
3. <Тесты и проверки.>

## Критерии приёмки

- [ ] <Проверяемое пользовательское поведение.>
- [ ] <Проверяемое состояние ошибки/пустых данных.>
- [ ] <Требование к responsive или accessibility.>
- [ ] Публичные API, фирменные цвета и несвязанные маршруты не изменены.

## Проверка

- **Unit:** `<команда>` или «не требуется: <причина>».
- **Lint:** `npx nx lint <проект>`.
- **Build:** `npx nx build admin`.
- **Ручная:** <короткий список действий и экранов>.

## Риски и открытые вопросы

- [ ] <Нерешённый продуктовый или технический вопрос.>

## История изменений

- YYYY-MM-DD: создана спецификация.
