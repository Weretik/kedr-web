# Phase 04 — React Native UI

Execute `TS-004`, `TS-005` and then `TS-006`. Reuse installed/approved package
primitives, current Paper theme and Ukrainian state copy.

**Exit checkpoint:** component tests cover gallery, all data rows, page states,
eligibility and successful cart confirmation.
