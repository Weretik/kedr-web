# Phase 07 — API integration

The API work is `TS-001` and `TS-002`, based on stable operation
`getPublicProductBySlug`. Run `npm run contracts:check`; do not create a parallel
handwritten transport schema.

**Exit checkpoint:** generated type, runtime schema, mapper and focused endpoint
tests agree with the versioned snapshot.
