# Phase 08 — verification

Execute `TS-007` then `TS-008`, followed by affected lint, tests, type checks,
Expo export and contract verification.

**Exit checkpoint:** every scenario links to actual evidence; unavailable iOS
or E2E environments remain explicit and are never reported as passed.
