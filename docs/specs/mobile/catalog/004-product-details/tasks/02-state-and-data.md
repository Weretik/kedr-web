# Phase 02 — state and data

Execute `TS-001` then `TS-002`: validate/map `ProductDetailsDto`, add the RTK
Query endpoint and expose domain page states without leaking generated types.

**Exit checkpoint:** data-access tests prove the exact `uk` request, mapping,
404, offline and retryable error behavior.
